{
  "exp_id": "hyp_003",
  "angle": "sanity",
  "stage": "val",
  "command": "OPTIMIZE",
  "seed": 42,
  "device": "cuda:0",
  "n_blocks": 8,
  "d_model": 128,
  "n_heads": 4,
  "ffn_mult": 4,
  "atom_type_emb_dim": 16,
  "dropout": 0.1,
  "alpha_pos": 0.05,
  "alpha_neg": 2.0,
  "shift_only": false,
  "use_actnorm": false,
  "n_steps": 2000,
  "batch_size": 128,
  "lr": 3e-4,
  "lr_schedule": "cosine",
  "warmup_steps": 200,
  "grad_clip_norm": 1.0,
  "val_interval": 200,
  "eval_n_samples": 500,
  "log_det_reg_weight": 1.0,
  "betas": [0.9, 0.999],
  "weight_decay": 1e-5,
  "use_ema": false,
  "augment_train": true,
  "normalize_to_unit_var": true,
  "data_root": "data/",
  "molecules": null,
  "wandb_project": "tnafmol",
  "wandb_group": "hyp_003",
  "wandb_tags": ["hypothesis", "hyp_003", "OPTIMIZE", "sanity", "val"],
  "wandb_notes": "SANITY validation: alpha_pos=0.05 (tighter), log_det_reg_weight=1.0 (10x stronger). Promising if valid_fraction > 0.1 on any molecule OR log_det_per_dof < 0.2.",
  "output_dir": "experiments/hypothesis/hyp_003_tarflow_stabilization"
}
